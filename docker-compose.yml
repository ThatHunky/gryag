version: "3.9"

services:
  bot:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    volumes:
      - ./:/app
      - models:/app/models # Persistent volume for local models
    restart: unless-stopped

volumes:
  models:
    driver: local
